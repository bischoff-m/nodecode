// TODO

// currently, nodecode-runtime is run directly from the command line
// this is not ideal, so this script should be implemented to run it from npm

// check if python & pip are installed, else throw error and open browser
// check if pipenv is installed, else ask if should be installed

// pipenv install?

// pipenv run python kernel_prototype? (would be nice to define entry point here)